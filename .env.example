# Market data and durable valuation snapshot store.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Server-only. Never expose this value through VITE_* variables or browser code.
SUPABASE_SERVICE_ROLE_KEY=

# Valuation Analysis sources. Keep all credentials server-side.
WISESHEETS_API_KEY=
SEC_USER_AGENT=Market Map contact@example.com
SEC_MAX_REQUESTS_PER_SECOND=6
SEC_CACHE_TTL_SECONDS=86400
SEC_FILING_CACHE_DIR=.cache/sec-filings

# Optional local company chat.
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen2.5:14b
WEB_SEARCH_ENABLED=false
WEB_SEARCH_PROVIDER=searxng
SEARXNG_BASE_URL=http://127.0.0.1:8080
WEB_FETCH_TOP_N=9

# Optional local earnings commentary generation.
EARNINGS_COMMENTARY_OLLAMA_ENABLED=false
EARNINGS_COMMENTARY_OLLAMA_TIMEOUT_MS=120000
EARNINGS_COMMENTARY_PIPELINE_TIMEOUT_MS=180000
EARNINGS_COMMENTARY_QNA_CONCURRENCY=2

# Optional production Basic Auth middleware.
SITE_USERNAME=dale
SITE_PASSWORD=
